# docker-compose.*.yml 의 image 태그(kaair-moveit:${ROS_DISTRO})를 결정합니다.
# scripts/build_docker.sh -t <humble|jazzy> 로 빌드한 태그와 일치시켜 주세요.
ROS_DISTRO=jazzy

# DDS 구현체 선택. 기본 rmw_cyclonedds_cpp, FastRTPS로 쓰려면 rmw_fastrtps_cpp 로 변경.
RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
